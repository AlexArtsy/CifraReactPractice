// import { Button } from '@/components/ui/button/button';

export const App = () => {
  return (
    <div className="p-4">
      <h1 className="text-2xl font-bold mb-4">Hello Vite + React + TS + Tailwind + Emotion!</h1>
      {/* <Button onClick={() => alert('Clicked!')}>Click me</Button> */}
    </div>
  );
};
