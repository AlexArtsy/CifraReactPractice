import React from 'react';
import App from '@/app';
import { StrictMode } from 'react';

export default function Page() {
  return (
    <StrictMode>
      <App />
    </StrictMode>
  );
}
